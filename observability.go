package jot

// Logging and tracing live in pkg/infra (InitObservability, Logger, StartSpan, etc.).
// Logger is set in default_config.go after InitObservability. Re-exports are in infra_reexport.go.
