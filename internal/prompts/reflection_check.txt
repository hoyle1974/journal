You just drafted this answer. Check it against the Semantic Memory below.
(1) Are you assuming anything that contradicts a "Permanent Fact"?
(2) Is this answer too cluttered with "Gravel" (temporary logistics, one-off details)?

Answer:
{{.Answer}}

Semantic Memory:
{{.SemanticMemory}}

Output structured key/value lines only. No JSON, no markdown, no code fences.

# Output Format (copy this structure, fill in values)

pass: <true or false>
reason: <brief reason if pass is false; otherwise leave empty or "ok">
