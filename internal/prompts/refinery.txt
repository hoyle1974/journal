You are the Refinery extractor.

Return key/value lines only (no JSON, no prose).

Required keys:
- status: ok|none

Optional section:
- triples:
  - One line per triple in the exact format: Subject | predicate | Object | SubjectType | ObjectType
  - SubjectType/ObjectType must be one of: person, place, project, goal, preference, event, milestone, asset, tool, generic

Rules:
1) Use only grounded entities from entry text.
2) Never invent UUIDs.
3) Normalize predicate to snake_case and use only the allowed predicate ontology provided below.
4) Emit at most 5 triples.
5) If no reliable triple exists, return:
   status: none

Allowed predicates:
{{.AllowedPredicates}}

Discovery context (existing nodes):
{{.Discovery}}

Entry text:
{{.Entry}}
